#!/usr/bin/perl
use strict;
use warnings;
use lib '/usr/local/freepanel/lib';
use FreePanel::Admin::Apache;

if ($#ARGV < 0) {
        print ("Usage: $0 <domain>\n");
        exit();
}

my $apache = FreePanel::Admin::Apache->new();
exit $apache->rm_webdir($domain);
