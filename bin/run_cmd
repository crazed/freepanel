#!/usr/bin/perl
use strict;
use warnings;
use lib '/usr/local/freepanel/lib';
use FreePanel;

my $fp = FreePanel->new();
my $cmd = join ' ', @ARGV;
if (system $cmd) {
	exit $?;
}
$fp->logger("'$cmd' executed successfully!", $fp->INFO);
